import 'package:flutter/material.dart';

class ForgeView extends StatelessWidget {
const ForgeView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}